---
import Section from "../Section.astro"
import { work as workData } from "@cv"
import type { Work } from "@/data/cv"
const work = workData as Work[]

// Group consecutive entries by company name
type WorkGroup = { name: string; url: string; description?: string; roles: Work[] }
const grouped: WorkGroup[] = []
for (const entry of work) {
  const last = grouped[grouped.length - 1]
  if (last && last.name === entry.name) {
    last.roles.push(entry)
  } else {
    grouped.push({
      name: entry.name,
      url: entry.url ?? "",
      description: entry.description,
      roles: [entry],
    })
  }
}
---

<Section title="Work">
  <ul>
    {
      grouped.map(({ name, url, description, roles }) => {
        const overallStart = new Date(roles[roles.length - 1].startDate).getFullYear()
        const overallEnd = roles[0].endDate
          ? new Date(roles[0].endDate).getFullYear()
          : "Present"

        return (
          <li>
            <article>
              <header class="company-header">
                <div class="company-meta">
                  <h3>
                    <a href={url} title={`View ${name}`} target="_blank" rel="noopener noreferrer">
                      {name}
                    </a>
                  </h3>
                  {description && <p class="company-description">{description}</p>}
                </div>
                <time>{overallStart} - {overallEnd}</time>
              </header>

              <div class="roles">
                {roles.map(({ position, startDate, endDate, summary, highlights }) => {
                  const startYear = new Date(startDate).getFullYear()
                  const endYear = endDate ? new Date(endDate).getFullYear() : "Present"

                  return (
                    <div class="role">
                      <header class="role-header">
                        <h4>{position}</h4>
                        {roles.length > 1 && <time>{startYear} - {endYear}</time>}
                      </header>
                      <footer>
                        {highlights && highlights.length > 0 ? (
                          <ul class="highlights">
                            {highlights.map((item) => (
                              <li>{item}</li>
                            ))}
                          </ul>
                        ) : (
                          summary && <p>{summary}</p>
                        )}
                      </footer>
                    </div>
                  )
                })}
              </div>
            </article>
          </li>
        )
      })
    }
  </ul>
</Section>

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  article h3 {
    font-weight: 500;
    color: #111;
  }

  article a {
    color: #111;
  }

  article a:hover {
    text-decoration: underline;
  }

  article h4 {
    color: #222;
    font-weight: 400;
  }

  .company-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 12px;
    margin-bottom: 8px;
  }

  .company-description {
    color: #777;
    font-size: 0.8rem;
    margin-top: 2px;
  }

  .company-header > time {
    color: #555;
    font-size: 0.85rem;
    min-width: 102px;
    text-align: right;
  }

  .roles {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .role-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 4px;
  }

  .role-header time {
    color: #555;
    font-size: 0.85rem;
    min-width: 102px;
    text-align: right;
  }

  .highlights {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding-left: 1.2rem;
    list-style: disc;
  }

  .highlights li {
    font-size: 0.85rem;
    color: #555;
    line-height: 1.5;
  }

  /* Stack the title/date rows on narrow phones so long names never collide
     with the fixed-width date column. */
  @media (width <= 480px) {
    .company-header,
    .role-header {
      flex-direction: column;
      gap: 2px;
    }

    .company-header > time,
    .role-header time {
      min-width: 0;
      text-align: left;
    }
  }
</style>
